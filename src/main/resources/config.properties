# config.properties

# Database configuration
db.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
db.user=sa
db.password=

# Cache configuration
cache.maxSize=100
cache.evictionStrategy=FIFO
cache.readPolicy=ReadThrough
cache.writePolicy=WriteAlways
